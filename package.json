{
  "name": "vote",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n api,client -c blue,green \"npm run dev:api\" \"npm run dev -w client\"",
    "dev:api": "cd backend && python3 -m uvicorn app.main:app --reload --host 127.0.0.1 --port 3000"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
